# About project

- It is because I need learning who works it paradigm.


## Step verifier to use in unit tests

- StepVerifier.create(...)
- Next
  - expectNext(...)
  - expectNextCount()
  - thenConsumerWhile(...)
- Verify
  - verifyComplete()
  - Verify(Duration)
  - VerifyError()
- StepVerifierOptions
  - Context
  - Scenario name
